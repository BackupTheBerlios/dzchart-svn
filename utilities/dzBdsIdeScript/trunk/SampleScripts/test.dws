var i: integer;
var s: string;

for i := 0 to IdeEditor.Count - 1 do begin
  s := Trim(IdeEditor[i]);
  if Copy(s, 1, 2) = '//' then
    IdeEditor[i] := Copy(s, 3, Length(s) - 2);
end;

for i := 0 to IdeEditor.Count - 1 do begin
  PrintLn(IdeEditor[i]);
end;
