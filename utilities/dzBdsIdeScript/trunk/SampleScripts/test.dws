var st: TStringList;
var IdeEditor: TIdeEditor;

st := TStringList.Create;
st.Text := TIdeEditor.GetSelectedText;
println(st.Count);
if st.Count > 2 then begin
  PrintLn('deleting second line');
  st.Delete(1);
end;

TIdeEditor.SetSelectedText(st.Text);
{
hallo
Welt
diedeldum
}
